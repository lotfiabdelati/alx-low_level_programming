#include <stdlib.h>
#include <stdio.h>
/**
 * main -entry point
 *
 * description: print all numbers
 *
 * Return: always 0 (success)
 */

int main(void)
{
	int a;

	for (a = 0; a  10 ; a++)
	{
	putchar(a + '0');
	if (i != 9)
	{
		putchar(',');
	}
}
	putchar('\n');
	return (0);
}
